

namein = 'in_length_100.wav';
nameout = 'out_length_100.wav';


[in, fin] = import_sound(namein);
[out, fout] = import_sound(nameout);
tin = (0:length(in)-1)/fin;
tout = (0:length(out)-1)/fout;

X = fftshift(fft(in));
Y = fftshift(fft(out));

H = Y./X;


